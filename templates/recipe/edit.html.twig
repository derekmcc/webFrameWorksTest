{% extends 'base.html.twig' %}

{% block title %}Edit Drink{% endblock %}

{% block body %}

    <h1 class="display-4">Edit Drink</h1>

    {% include 'recipe/_form.html.twig' with {'form': form, 'button_label': 'Edit Drink'} only %}
    <br>
    <div class="row">
         <a href="{{ path('recipe_index') }}" class="btn btn-outline-dark" style="margin-right: 1%">Back to Drinks</a>
        {% include 'recipe/_delete_form.html.twig' with {'identifier': recipe.id} only %}
    </div>
    <br>
    <br>
{% endblock %}