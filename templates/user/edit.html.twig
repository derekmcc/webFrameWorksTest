{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}

    <h1 class="display-4">Edit User Account</h1>

    {% include 'user/_form.html.twig' with {'form': form, 'button_label': 'Save Changes'} only %}
    <br>
    <div class="row">
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('user_index') }}" class="btn btn-outline-dark" style="margin-right: 1%">Back to list</a>
    {% endif %}

    {% include 'user/_delete_form.html.twig' with {'identifier': user.id} only %}
    </div>
    <br>
    <br>
{% endblock %}